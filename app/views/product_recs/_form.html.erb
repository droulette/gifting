<%= form_for(@product_rec) do |f| %>
  <% if @product_rec.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_rec.errors.count, "error") %> prohibited this product_rec from being saved:</h2>

      <ul>
      <% @product_rec.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :product %><br />
    <%= f.text_field :product %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
