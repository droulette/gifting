<h1>Listing product_recs</h1>

<table>
  <tr>
    <th>User</th>
    <th>Product</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_recs.each do |product_rec| %>
  <tr>
    <td><%= product_rec.user %></td>
    <td><%= product_rec.product %></td>
    <td><%= link_to 'Show', product_rec %></td>
    <td><%= link_to 'Edit', edit_product_rec_path(product_rec) %></td>
    <td><%= link_to 'Destroy', product_rec, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product rec', new_product_rec_path %>
